Implemented ZEP-170 by narrowing admin checks to membership role names only (plus superuser).
Removed checks against `user.role_name`, `user.role`, and groups to avoid over-permissive access.
Added permission unit tests for membership allow, memberships list allow, and non-membership role deny.
Tests: `pytest -q` passed.
Lint: `python -m ruff check .` failed (ruff not installed).
Files changed: `zephyr_xml_importer/api/permissions.py`.
Files changed: `tests/test_permissions.py`.
Worker report: `.agent/worker_report.md`.
Test log: `.agent/logs/test_${CODEX_ITERATION}.log`.
Lint log: `.agent/logs/lint_${CODEX_ITERATION}.log`.