Selected task: ZEP-170 — Admin role check: restrict to TestY membership role if required.
Updated plan file: `.agent/plan.md`.
Appended progress entry to `.agent/progress.md` (CODEX_ITERATION=6).
State file unchanged: `.agent/state.md`.
Queue edits: none.
Worker next: review spec security requirement vs current permission logic in `zephyr_xml_importer/api/permissions.py`.
Worker next: narrow role checks to membership role(s) only, plus `is_superuser`.
Worker next: keep `ADMIN_ROLE_NAME` fallback default "Admin".
Worker next: add/update unit tests in `tests/test_permissions.py` for membership allow and non-membership deny.
Worker next: run `pytest -q`.
Worker next: run `python -m ruff check .` if available.